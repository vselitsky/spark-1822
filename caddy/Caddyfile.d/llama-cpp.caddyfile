llama.{$CADDY_DOMAIN} {
	tls internal

	encode zstd gzip
	import common_headers
	import default_log

	reverse_proxy llama-cpp:8080 {
		# llama-server streams tokens; disable proxy buffering.
		flush_interval -1
	}
}
